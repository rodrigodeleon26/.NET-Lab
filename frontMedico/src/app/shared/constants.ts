export const TOKEN_KEY = 'token_medico';

export const REFRESH_TOKEN_KEY = 'refreshToken_medico';

export const environment = {
    production: false,
    AuthWebApiBaseUrl: 'https://localhost/auth/api'
  };